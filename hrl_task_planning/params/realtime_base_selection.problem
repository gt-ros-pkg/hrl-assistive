(DEFINE
(PROBLEM test_problem)
(:DOMAIN realtime_base_selection)
(:OBJECTS)
(:INIT  (NOT (AT BASE_GOAL))
)
(:GOAL (AND (AT BASE_GOAL)
            (NOT(TORSO_SET TORSO_GOAL))
            (NOT(KNOWN TORSO_GOAL))
            (NOT(KNOWN BASE_GOAL))
            (NOT(KNOWN EE_GOAL))
        ))
)
