var RFH = (function (module) {
    module.SkinDisplay = function (options) {
        'use strict';
        var self = this;
        var camera = options.camera;
        var displayContactAlert = function (contactPoint) {
            
        };

    };
    return module;
})(RFH || {});
